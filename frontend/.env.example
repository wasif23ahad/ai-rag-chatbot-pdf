# API base URL — used by Axios in the React frontend
# In development, Vite proxies /api to this URL automatically (see vite.config.js)
# In production (Docker), Nginx handles the proxy — set to empty or backend URL
VITE_API_URL=http://localhost:8000
